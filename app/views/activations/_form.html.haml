= hidden_field_tag :invite, params[:invite]
= hidden_field_tag :activation_code, params[:activation_code]
-@chpwd ||= false
-if @chpwd
  .clearfix
    = form.label :old_password, "Old Password"
    = password_field_tag :old_password, nil, :size=>30
-unless params[:invite].blank?
  .clearfix
    = form.label :login, "Login"
    = form.text_field :login, value: ''
.clearfix
  = form.label :password, "#{(@chpwd) ? 'New p' : 'P'}assword<span class='label_desc'>min. size 4 chars</span>".html_safe
  = form.password_field :password
.clearfix
  = form.label :password_confirmation, "#{(@chpwd) ? 'New p' : 'P'}assword again<span class='label_desc'>type the password again</span>".html_safe
  = form.password_field :password_confirmation
-#
-#  #or(style='width:100%;')
-#    Or
-#  .clearfix
-#    =form.label :openid_identifier, "OpenID<span class='label_desc'>use OpenID instead of your email / password</span>".html_safe
-#    =form.text_field :openid_identifier
