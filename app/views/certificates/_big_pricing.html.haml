-if (current_user && current_user.ssl_account.is_registered_reseller?) || cookies[:r_tier]
  .reseller_price_starting_at as low as #{pricing(@certificate)}
-else
  .price_starting_at as low as #{pricing(@certificate)}/yr
