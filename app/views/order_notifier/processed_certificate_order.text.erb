<%cc=@certificate_order.certificate_content%>
<%if @contact.is_a? Contact -%>
Dear <%=[@contact.first_name, @contact.last_name].join(" ")%>,

<%end -%>
Thank you for placing your <%=Settings.community_name%> <%=@certificate_order.certificate.description["certificate_type"]%> certificate order for <%=@certificate_order.subject%>.

Attached to this email you should find a .zip file containing:

  <%@certificate_order.bundled_cert_names.each do |cc|%>
  <%if @certificate_order.ca == CertificateOrder::CA_CERTIFICATES[:SSLcomSHA2] -%>
    * <%=Certificate::BUNDLES[:comodo][:sha2_sslcom_2014][:labels][cc]%> - <%=cc%>
  <%else-%>
    * <%=Certificate::COMODO_BUNDLES[cc]%> - <%=cc%>
  <%end-%>
  <%end-%>
    * Your Certificate (<%=@signed_certificate.file_type%>) - <%=@signed_certificate.friendly_common_name%><%=@signed_certificate.file_extension%>

Certificate files formatted for your specific server platform can also be downloaded directly from your certificate order page. More information can be found in the link below:

  https://www.ssl.com/article/certificate-order-page-information/

<%if @certificate_order.certificate.is_ev? && !@signed_certificate.is_ev?-%>
The above DV certificate serves as a placeholder so that you can begin securing your site right away.

However, in order to enable your EV certificate (and display the green address bar on your site) we'll need a bit more information.

Please submit the document items outlined in the link below through your SSL.com portal:
  
  https://info.ssl.com/faq-requirements-for-ssl-com-ev/
<%end-%>
<%if (@certificate_order.certificate_duration(:days).to_i > 1187) && (@certificate_order.created_at > Date.parse('Apr 1 2015'))-%>

  <%=render(:partial=>"duration_limit")%>
<%end-%>

Build more trust and transparency with your customers by displaying your SSL.com Smart SeaL. Get your FREE secure Smart SeaL by visiting the following link:

  <%=certificate_order_site_seal_url(@certificate_order)%>.


Again, thank you for choosing SSL.com, where we believe a safer internet is a better internet.


Kind regards,

SSL.com Support Team

https://info.ssl.com/

https://support.ssl.com/default_import/Tickets/Submit

<%if @certificate_order.is_iis?%>
<%=@signed_certificate.to_pkcs7%>
<%elsif @certificate_order.is_nginx?-%>
<%=@signed_certificate.to_nginx%>
<%else-%>
<%=@signed_certificate.body%>
<%end-%>