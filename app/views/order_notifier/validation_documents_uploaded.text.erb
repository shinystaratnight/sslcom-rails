Hello,

The following validation documents for <%=@certificate_order.subject%> (SSL.com certificate ref# <%=@certificate_order.ref%>) have been recently uploaded:

<%@files.each do |file|%>
  <%=file.original_filename%>
<%end%>

We will promptly process these files.

<%=render(:partial=>"support_contact")%>

