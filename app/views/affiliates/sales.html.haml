-c={}
-c[:title] = "SSL.com Affiliate Sales and Statistics"
-c.each do |k, v|
  -content_for k, c[k]
=render :partial => '/surls/index_scripts'
.innercontainere
  .inner
    %h1
      =c[:title]
    %h2
      information displayed is for affiliate
      %strong #{current_user.login}
    .clearfix
      -if @stats.empty?
        %h2
          no sales or statistics have been recorded yet. Please visit the links page to get started.
      -else
        %table#manage_current_links(cellspacing=0)
          %tr
            %th referral link
            %th visits
            %th number sales
            %th total sales
            %th commission
          -@stats.each do |k, v|
            =render partial: "affiliate_row", locals: {url: k, orders: v}

